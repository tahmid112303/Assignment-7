import React from 'react';

const TaskStatus = ({visible}) => {
    return (
        <div className={`w-[250px] h-[150px] bg-white ml-[20px] ${visible}`}>
            
        </div>
    );
};

export default TaskStatus;